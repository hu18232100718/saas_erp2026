   # 统一表单与交互规范（ERP，最新去重版）

## 0. 生效原则
- 本文档为唯一生效版本。
- 历史重复规则按“最新要求优先”合并，本文已去重。
- 后续新增规则必须追加到本文，不再分散到其他临时文档。

## 1. 页面与弹窗布局
- 列表页取消查询区上方页面标题栏（`h2`）。
- 按钮栏统一放在“查询区域与列表区域之间”，靠左排列。
- 按钮顺序统一：`新增` -> `导出` -> `导入` -> `自定义列`。
- 内容主区上边距紧凑（贴近菜单标签栏）。
- 新增/编辑弹窗默认定位在菜单标签栏下方（`unified-edit-dialog`）。
- 单据新增/编辑优先使用“页面标签模式”，少用弹窗（按业务复杂度执行）。

## 2. 查询区规范
- 查询区统一使用 `search-card`。
- 桌面端 100% 缩放默认优先 4 列显示，宽度不足再换行。
- 搜索/重置按钮作为普通查询项，紧跟最后一个条件，不独占整行。
- 输入框回车必须触发当前页面查询（全条件组合查询）。
- 下拉框宽度固定，与输入框统一；选中值失焦后不得消失。
- 下拉多选统一使用复选样式（含“全选”“已选X项”）。
- 数量/金额查询统一使用“比较符 + 数值”一体控件，占一个标准查询位宽。
- 审计字段默认不作为查询条件。

## 3. 列表按钮与操作列
- 按钮栏间距统一使用 `gap`，禁止页面自行叠加 `margin-left`。
- 操作列统一为“编辑 + 更多”两段式。
- 删除、日志及其他操作放入“更多”下拉。
- “更多”文本后显示下拉箭头。
- 操作区右对齐，列宽紧凑（仅容纳两个按钮）。
- 危险操作（如删除）在更多菜单中使用红色强调。

## 4. 列表结构与样式
- 主列表首列：复选框列（当页全选）。
- 第二列：序号列（从 1 开始，排序后仍连续）。
- 表头禁止换行；上下垂直居中；不保留额外上边距。
- 开启表格竖边框（表头/内容/合计区一致）。
- 主列表 `el-table` 使用 `fit=true`，按可视区自适应，减少右侧留白。
- 数量类列宽统一紧凑（默认 `92px`，最多 `96px`）。
- 查询区与列表区字号统一约 `13px`，表头加粗。

## 5. 列表分页与合计
- 分页区统一右对齐，紧凑尺寸（每页下拉约 `24px` 高、`96px` 宽）。
- 分页区与表格主体间保留明确间距。
- 合计统一两行显示：`页小计`、`总合计`。
- 合计标签与数值统一红色高亮。
- 页小计/总合计之间使用整行分割线。
- 可合计字段仅限数量/金额类型；外键字段禁止合计。
- 合计与自定义列联动：列未显示时，不允许显示该列合计。

## 6. 自定义列统一规则
- 自定义列配置按“租户 + 用户”维度持久化。
- 支持：列显示/隐藏、排序、查询条件、合计显示。
- 设置为查询条件后必须立即生效并出现在查询区。
- 新增功能页面必须默认接入自定义列，不可缺省。

## 7. 新增/编辑表单统一规范
- 统一 4 列编辑网格：`unified-form-grid`。
- 默认每项占 1 列；跨列时使用 `field-span-2`、`field-span-4`。
- 勾选项同样走 4 列，不允许退回 2 列布局。
- 行间距、分区间距统一紧凑（压缩上下空隙）。
- 标签禁止换行；超长单行省略或扩标签宽度。
- 备注默认单行并可占 2 列；确有需要再改多行。
- 新增界面不自动聚焦特定字段（用户手动点击）。
- 字符限制统一显示 `当前/总数`（如 `0/64`），不再写“最多X字符”。

## 8. 查看态与只读态规范
- 查看态不显示 placeholder（“请输入/请选择”），空值显示空白或 `-`。
- 只读/禁用字段文本颜色与可编辑字段一致，不允许发灰不可读。
- 只读态优先使用“文本回显”策略，避免下拉值空白。
- 需要查看关联信息时，使用预览弹窗，不做页面跳转。

## 9. 审计信息规范
- 新增界面不显示审计信息。
- 编辑/查看显示：创建人、创建时间、修改人、修改时间。
- 有审核流程时增加：审核人、审核时间。
- 有入库/出库流程时增加：入库人/入库时间、出库人/出库时间。

## 10. 菜单与导航规范
- 左侧一级菜单右侧显示 `>`，右对齐；折叠时隐藏。
- 二级悬浮菜单贴顶显示，不留顶部空白。
- 二级悬浮菜单宽高使用统一变量（含响应式断点）。
- 顶部面包屑按三级菜单展示：`一级-二级-三级`。
- 功能标签页最多 15 个；清空标签后回到数据看板页面（不保留固定看板标签）。

## 11. 图片与附件（SaaS隔离）
- 目录统一：`tenant_files/tenant_{tenantId}/业务目录`。
- 商品图片目录示例：`biz_product_IMG`。
- 商品图片支持 `JPG/JPEG/PNG`，最大 10MB，每款 1 张。
- 文件名：`业务编码 + 原后缀`。
- 上传时自动建目录；删除图片时同步删除物理文件；可重新上传。
- 列表与明细缩略图统一紧凑尺寸并居中（行内上下左右居中）。

## 12. 导入/导出规范
- 只要有“新增”按钮，必须同时有“导入”能力。
- 导入弹窗统一：下载模板、上传文件、结果统计、失败明细、失败下载。
- 导入完成后弹窗不自动关闭。
- 模板必填字段红色标识。
- 外键导入支持名称识别；重名时必须提示使用编码。
- 导出规则：勾选行则导出勾选；未勾选则导出当前查询结果。

## 13. 单据通用交互
- 保存后不关闭界面，直接进入编辑态。
- 审核按钮在保存按钮旁；审核成功后关闭当前标签并返回列表。
- 点击审核时，系统必须先自动保存当前编辑改动；保存失败则中断审核并提示失败原因。
- 返回列表按钮固定最右侧。
- 返回列表时如有未保存修改：弹窗支持“保存并返回 / 不保存返回 / 取消”。
- 单据编号支持双击复制（全局规则）。
- 外键关联值支持“眼睛图标预览弹窗”。

## 14. 单据状态与权限
- 已审核：列表蓝色显示；只允许查看。
- 已作废：列表斜体显示；只允许查看。
- 草稿：允许编辑；删除仅草稿可见。
- 详情页盖章放在主表右上角：
  - 已审核：红色斜体盖章
  - 已作废：灰色斜体盖章
- 往来信息 4 个单据不允许取消审核。

## 15. 单据明细录入与空表样式
- 明细空表统一渲染 10 行占位行，不允许手工背景线模拟。
- 占位实现统一复用：`useDetailPlaceholderRows.ts`。
- 单据详情明细区固定高度展示，不随行数缩放，审计区固定贴底。
- 无明细时显示空网格，不显示“暂无数据”。
- 明细行高紧凑；图片列不拉高行高。
- 新增明细全局样式必须保证“表头列边界与明细列边界完全对齐”。

## 16. 条码候选悬浮录入
- 条码回车匹配多条时显示悬浮候选表（分页默认 10 行）。
- 候选表数量输入后回车：仅写入数量 > 0 的行。
- 写入后自动关闭候选、清空录入框并重新聚焦。
- 候选层挂到 `body`（最高层），不被底层滚动区域遮挡。
- 点击候选框外部自动关闭。
- 条码降级候选场景禁止重复错误弹窗。

## 17. 数量/金额业务规则
- 数量/金额可配置为查询条件，支持 `> >= = <= <`。
- 输入非数字必须阻止查询并提示。
- 后端按真实数值比较，不做字符串比较。
- 采购订单明细保留并自动计算：预入量、已入量、剩余量。
- 采购到货/入库/退货等联动时，数量校验在失焦即触发，不等待保存。

## 18. 成本核算口径
- 系统参数 `月结方式=1` 时，启用移动加权平均成本。
- 系统参数 `月结方式=2` 时，启用月末一次加权平均成本。
- 当前阶段月末成本仅计算“总部单品成本”，不计算客户单品成本。
- 总部单品成本的月末公式固定为：
  - `月末单品成本 = （期初库存成本金额 + 本期采购入库金额 - 本期采购退货出库金额） / （期初库存数量 + 本期采购入库数量 - 本期采购退货出库数量）`
- 期初库存数量：
  - 取“上月月末时点”的总部库存数量。
  - 仅统计 `店仓档案所属经销商级别=总部` 的库存。
- 期初库存成本金额：
  - `期初库存数量 × 上月历史月成本`
  - 若上月无历史月成本，则取商品档案成本价。
- 本期采购入库数量/金额：
  - 取本月总部“已完成采购入库单”的汇总数量与汇总金额。
- 本期采购退货出库数量/金额：
  - 取本月总部“已完成采购退货出库单”的汇总数量与汇总金额。
- 历史月成本：
  - 财务月结审核成功后写入。
  - 取消月结后删除当月历史月成本，并回退当前总部单品成本。
- 月末成本的参与业务范围固定为：
  - 参与单品成本重算：`采购入库`、`采购退货`
  - 不参与单品成本重算：`调拨`、`盘点`、`生产`、`销售退回`、`销售出库`、其他库存调整
- 调拨、盘点、生产、销售退回等业务：
  - 仅影响库存数量、库存流水、期末库存报表、期末库存成本金额报表
  - 不改变单品成本来源口径
- 期末库存成本金额报表口径固定为：
  - `期末库存成本金额 = 期末库存数量 × 月末单品成本`

## 19. 编码与乱码防护
- 前后端源码、SQL、模板文案统一 UTF-8。
- 前端构建前必须执行：`npm run check:encoding`。
- `prebuild` 必须自动执行编码检查，失败即阻断打包。
- 禁止提交乱码文案到页面标题、字段名、状态名、提示语。

## 20. 本地启停与打包
- 本地统一流程固定为：`停止 -> 打包 -> 启动 -> 状态检查`，不得跳过停服务直接打包。
- 重启必须分三条命令串行执行，不允许把启动和状态检查并成一条长命令：
  - `& 'E:\\SAAS_ERP\\scripts\\Stop-Jar.ps1'`
  - `& 'E:\\SAAS_ERP\\scripts\\Start-Jar.ps1' -HealthWaitSeconds 0`
  - `& 'E:\\SAAS_ERP\\scripts\\Status-Jar.ps1'`
- 打包必须分两步执行：
  - `& 'E:\\SAAS_ERP\\scripts\\Stop-Jar.ps1'`
  - `& 'E:\\SAAS_ERP\\scripts\\Package-Jar.ps1'`
- `Start-Jar.ps1` 必须自动结束，不长期阻塞终端；若服务已在运行，应直接输出当前状态并退出。
- `Stop-Jar.ps1`、`Status-Jar.ps1` 必须以真实端口 `55322` 与真实 Java 进程为准，不能只依赖 `.single-jar.pid`。
- `Package-Jar.ps1` 必须生成可执行包 `inventory-pro-*-exec.jar`，并发布到：
  - `E:\\SAAS_ERP\\release\\inventory-pro-latest.jar`
- 本地运行统一使用 `release\inventory-pro-latest.jar`，不得直接依赖 `target` 目录中的运行包，避免 Windows 文件锁导致打包失败。
- `release` 目录只保留最新 3 个历史归档包，超出后自动清理。
## 21. 版本与发布
- 重大改动必须升级版本号（前后端一致）。
- 版本号显示在左侧菜单底部，以前端 `package.json` 为展示源。
- 发布包目录仅保留最近 3 个历史包，超出自动清理。

## 22. 删除与日志治理
- 删除业务数据时需同步清理对应修改日志，避免垃圾数据累积。
- 系统必须保留“操作日志”（删除操作留痕），按租户隔离存储。
- 记录项至少包含：菜单、关键标识（编号/名称/单号）、操作人、操作时间、租户。

## 23. 开发上线前自检清单（必检）
### 23.1 页面结构与样式
- [ ] 查询区为 `search-card`，100%缩放默认4列优先显示。
- [ ] 查询按钮紧跟最后一个条件，不独占整行。
- [ ] 按钮栏顺序为：新增 -> 导出 -> 导入 -> 自定义列。
- [ ] 列表操作列为“编辑 + 更多”，删除/日志在更多内。
- [ ] 表头不换行、上下居中，表格有竖边框。
- [ ] 数量类列宽使用紧凑宽度（92~96px）。
- [ ] 分页区右对齐、紧凑尺寸；与表格有间距。
- [ ] 合计为两行（页小计/总合计），红色高亮，带分割线。

### 23.2 表单与查看态
- [ ] 新增/编辑使用4列表单网格，勾选项同样4列。
- [ ] 字符限制显示 `当前/总数`（如 `0/64`）。
- [ ] 新增页不显示审计信息。
- [ ] 查看态不显示 placeholder；空值显示空白或 `-`。
- [ ] 只读/禁用字段文本颜色与可编辑字段一致。
- [ ] 下拉只读态使用文本回显，不允许出现空白值。

### 23.3 交互行为
- [ ] 查询输入框按回车可直接触发查询。
- [ ] 单据保存后不关闭，进入编辑态。
- [ ] 审核成功后关闭当前标签并返回列表。
- [ ] 返回列表前若有未保存修改，弹窗支持“保存并返回/不保存返回/取消”。
- [ ] 单据编号支持双击复制。
- [ ] 外键支持眼睛预览弹窗，不做页面跳转。

### 23.4 权限与状态
- [ ] 删除按钮仅草稿状态显示。
- [ ] 已审核列表蓝色、已作废列表斜体。
- [ ] 已审核/已作废为只读查看，不可编辑。
- [ ] 详情页盖章在主表右上角（红色已审核、灰色已作废）。

### 23.5 导入导出与自定义列
- [ ] 有新增按钮的页面必须有导入按钮与导入能力。
- [ ] 导入弹窗显示总数/成功/失败/失败明细，且不自动关闭。
- [ ] 导入模板必填字段红色标识。
- [ ] 外键导入支持名称识别，重名提示改用编码。
- [ ] 自定义列可配置显示、查询条件、合计，并按“租户+用户”保存。
- [ ] 设置为查询条件后，查询区立即生效。

### 23.6 单据明细
- [ ] 明细空表渲染10行占位，表头与明细列边界完全对齐。
- [ ] 明细固定高度展示，审计区固定贴底。
- [ ] 明细行高紧凑，图片缩略图不拉高行高。
- [ ] 条码候选悬浮录入可分页（默认10行）、可外部点击关闭、可批量回车写入数量>0行。

### 23.7 数据与安全
- [ ] 所有查询、列表、新增、编辑、删除按租户隔离。
- [ ] 超级用户禁用限制、关键控制规则已生效（按业务模块要求）。
- [ ] 删除业务数据时同步清理对应修改日志。
- [ ] 删除操作写入“操作日志”（菜单、标识、操作人、时间、租户）。

### 23.8 成本核算
- [ ] `月结方式=1` 时仅启用移动加权平均成本。
- [ ] `月结方式=2` 时仅启用月末一次加权平均成本。
- [ ] 当前阶段月末成本仅计算总部单品成本。
- [ ] 月末单品成本公式与“期初 + 本期采购入库 - 本期采购退货”标准口径一致。
- [ ] 期初库存数量取上月月末时点总部库存，不混入非总部库存。
- [ ] 上月无历史月成本时，期初成本回退商品档案成本价。
- [ ] 调拨、盘点、生产、销售退回等业务不参与单品成本重算。
- [ ] 期末库存成本金额按 `期末库存数量 × 月末单品成本` 计算。

### 23.9 编码、防乱码与发布
- [ ] 新增文案、SQL、模板均为 UTF-8 编码。
- [ ] 本地构建前执行 `npm run check:encoding` 并通过。
- [ ] 本地重启按三步执行：Stop -> Start(HealthWaitSeconds 0) -> Status。
- [ ] 打包前先停服务，打包后启动并检查健康接口200。
- [ ] 发布包目录仅保留最近3个历史包。

## 24. 页面说明帮助统一规范（最新）
- 适用范围：成本管理、月结、库存、往来、复杂单据等存在业务规则说明的页面。
- 默认规则：页面说明信息默认收起，不允许以整块说明卡默认展开占用页面空间。
- 统一交互：使用“问号说明图标 + 点击弹出说明”的方式展示。
- 图标位置：统一放在列表页按钮区最末尾，位于 `导出 / 导入 / 自定义列` 等按钮之后。
- 弹出方式：点击图标显示说明弹层，点击空白区域自动关闭；不使用纯鼠标悬停 Tooltip 作为主要交互。
- 说明内容：默认仅保留“当前月结方式/当前成本方式、算法说明、提示”三类信息；避免展开过多字段口径和参与业务明细。
- 复用要求：后续新页面如需业务说明，优先复用统一组件 `CostRuleHelp.vue`，不得各页面单独发明新样式。
- 视觉要求：问号按钮尺寸与按钮区其他控件协调，默认放在按钮区最后，不抢占主要业务按钮的视觉优先级。
